goal:
  input_type: grid
  row: null
  col: null
  x_m: null
  z_m: null

attractive:
  gain: 1.0
  mode: quadratic
  saturation_distance_m: 20.0

repulsive:
  gain: 5.0
  influence_radius_m: 3.0

combination:
  cost_weight: 1.0
  occupied_potential: 1000000.0
  unknown_policy: blocked
  normalize_output: true

output:
  attractive_dir: outputs/potential/attractive
  repulsive_dir: outputs/potential/repulsive
  combined_dir: outputs/potential/combined
  gradient_dir: outputs/potential/gradients
  visualization_dir: outputs/potential/visualizations

runtime:
  save_npy: true
  save_png: true
  save_gradient: true
  save_visualizations: true
